# bb5 — Canopy Goods garden skin

- Remapped `--ocean-*` tokens to soil/leaf/citrus (names kept so templates/admin keep working).
- `theme.css`: greenhouse hero (Unsplash foliage), leaf logo mark, citrus CTAs, product card hover.
- Brand strings stay in `config.js` / admin locals — not hard-coded in CSS/HTML.

## Pothos feature pass
- Centered single-card layout, taller media for window-hang art
- Show product description on solo cards
- Hero uses `/uploads/pothos-window-hang.png`

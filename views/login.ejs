<%- include('partials/header') %>

  <p class="auth-kicker">Account</p>
  <h1 class="page-title">Welcome back</h1>
  <p class="page-sub">Return customers sign in with the email on the account. New here? Register takes a minute.</p>

  <div class="panel">
    <% if (typeof invalid !== 'undefined' && invalid) { %>
      <div class="alert alert-error">Email or password is incorrect.</div>
    <% } %>

    <form method="post" action="/login">
      <%- include('partials/csrf') %>
      <div class="form-group">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" required autocomplete="email" value="<%= typeof email !== 'undefined' ? email : '' %>">
      </div>
      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" required autocomplete="current-password">
      </div>
      <div class="actions">
        <button type="submit" class="btn btn-primary">Sign in</button>
        <a class="btn btn-secondary" href="/register">Create account</a>
      </div>
      <p class="page-sub" style="margin-top:1rem;margin-bottom:0;"><a href="/forgot-password">Forgot password?</a></p>
    </form>
  </div>

<%- include('partials/footer') %>
